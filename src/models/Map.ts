export interface Position {
  lat: number;
  lng: number;
}

export interface Place {
  x: number;
  y: number;
  address_name: string;
  phone: string;
  place_name: string;
  place_url: string;
  category_name: string;
}
